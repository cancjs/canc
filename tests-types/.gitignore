# Generated by scripts/run-matrix.mjs — packed package tarballs + per-version
# fixture projects (package.json, tsconfig.json, node_modules). Regenerated on
# every matrix run; only the shared fixtures/common/*.ts sources + the runner
# and matrix.config.json are source-controlled.
.tarballs/
fixtures/ts-*/
